import React from 'react'
import { useParams } from "react-router-dom";

const EditRole = () => {
  const { id } = useParams();
  return (
    
    <div>
      
    </div>
  )
}

export default EditRole